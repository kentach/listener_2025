<div class="vocab-page">
  <!-- 左上戻るボタン（固定ではない） -->
  <%= link_to "＜英検級に戻る", vocabularies_path, class: "btn-back" %>

  <!-- ヘッダー -->
  <header class="vocab-header">
    <h2>英検<%= @level %> 重要単語リスト</h2>
    <p>全<%= @vocabularies.count %>語</p>
  </header>

  <!-- 検索バー -->
  <div class="search-wrapper">
    <%= form_with url: level_vocabularies_path(level: @level), method: :get, local: true, class: "vocab-search-form" do %>
      <input type="text" name="q" placeholder="単語を検索..." value="<%= params[:q] %>">
      <button type="submit">🔍</button>
    <% end %>

    <% if params[:q].present? %>
      <%= link_to "元のリストに戻る", level_vocabularies_path(level: @level), class: "btn-return" %>
    <% end %>
  </div>

  <!-- シリーズごとの単語リスト -->
  <% @vocabularies.group_by(&:series).each do |series, words| %>
    <div class="series-block">
      <div class="series-header"
           onclick="this.nextElementSibling.classList.toggle('open');
                    this.querySelector('.toggle-icon').textContent =
                    this.nextElementSibling.classList.contains('open') ? '−' : '+';">
        <span><%= series %>（<%= words.count %>語）</span>
        <span class="toggle-icon">+</span>
      </div>

      <div class="series-words">
        <!-- アクションボタン -->
        <div class="series-actions">
          <%= link_to "暗記を始める",
              memorization_vocabularies_path(level: @level, series: series),
              class: "btn-action btn-memo" %>

          <%= button_to "テストに挑戦",
                tests_path,
                method: :post,
                params: {
                  series: series,
                  level: @level,
                  range_start: words.first.number,
                  range_end: words.last.number,
                  question_language: "english"
                },
                class: "btn-action btn-test" %>
        </div>

        <!-- 単語カード -->
        <% words.each do |word| %>
          <div class="word-card">
            <div class="word-info">
              <span class="word-number"><%= word.number %>.</span>
              <span class="word-english"><%= word.english %></span>
              <span class="word-japanese"><%= word.japanese %></span>
            </div>
            <button class="play-btn">🔊</button>
          </div>
        <% end %>
      </div>
    </div>
  <% end %>
</div>

<%= render "shared/navigation_tab" %>

<style>
/* === ページ全体 === */
.vocab-page {
  max-width: 600px;
  margin: 0 auto;
  font-family: sans-serif;
  padding: 16px;
  padding-bottom: 120px; /* タブバー分の余白 */
  box-sizing: border-box;
}

/* 左上戻るボタン */
.btn-back {
  display: inline-block;
  margin-bottom: 16px;
  padding: 8px 16px;
  background-color: #ccc;
  border-radius: 8px;
  color: #333;
  font-weight: bold;
  text-decoration: none;
}
.btn-back:hover { background-color: #aaa; }

/* ヘッダー */
.vocab-header {
  text-align: center;
  margin-bottom: 20px;
}
.vocab-header h2 { font-size: 22px; font-weight: bold; }
.vocab-header p { font-size: 16px; color: #555; }

/* 検索バー */
.search-wrapper {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 8px;
  margin-bottom: 16px;
}
.search-wrapper input {
  flex: 1 1 200px;
  padding: 8px 12px;
  border-radius: 8px;
  border: 1px solid #ccc;
}
.search-wrapper button {
  padding: 8px 16px;
  border-radius: 8px;
  border: none;
  background-color: #2196F3;
  color: #fff;
  cursor: pointer;
}
.btn-return {
  display: inline-block;
  padding: 8px 16px;
  background-color: #9E9E9E;
  border-radius: 8px;
  color: #fff;
  text-decoration: none;
}
.btn-return:hover { background-color: #757575; }

/* シリーズブロック */
.series-block {
  margin-bottom: 24px;
  border-radius: 12px;
  overflow: hidden;
  box-shadow: 0 3px 8px rgba(0,0,0,0.12);
}
.series-header {
  color: #fff;
  background-color: #333;
  padding: 12px 16px;
  font-weight: bold;
  font-size: 16px;
  display: flex;
  justify-content: space-between;
  cursor: pointer;
}
.series-words {
  display: none;
  background-color: #f7f7f7;
  padding: 12px;
}
.series-words.open { display: block; }

/* === アクションボタン === */
.series-actions {
  display: flex;
  justify-content: center;
  gap: 12px;
  margin-bottom: 12px;
  flex-wrap: wrap;
}
.btn-action {
  flex: 1 1 48%;
  width: 150px;
  padding: 12px 0;
  border-radius: 8px;
  font-weight: bold;
  color: #fff;
  text-decoration: none;
  border: none;
  cursor: pointer;
  text-align: center;
  transition: background-color 0.2s;
  display: flex;
  align-items: center;
  justify-content: center;
}
.btn-memo { background-color: #f87b0e; }
.btn-memo:hover { background-color: #c66613; }
.btn-test { background-color: #2196F3; }
.btn-test:hover { background-color: #1976D2; }

/* 単語カード */
.word-card {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 10px 16px;
  margin-bottom: 8px;
  border-radius: 8px;
  background-color: #fff;
  box-shadow: 0 1px 4px rgba(0,0,0,0.08);
  transition: transform 0.1s;
}
.word-card:hover { transform: translateY(-1px); }
.word-info {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  align-items: center;
}
.word-number { font-size: 14px; color: #888; }
.word-english { font-size: 16px; font-weight: 600; color: #222; }
.word-japanese { font-size: 14px; color: #555; }

/* 発音ボタン */
.play-btn {
  padding: 6px 12px;
  font-size: 14px;
  border: none;
  border-radius: 6px;
  background-color: #2196F3;
  color: #fff;
  cursor: pointer;
}
.play-btn:hover { background-color: #1976D2; }

/* レスポンシブ */
@media (max-width: 600px) {
  .series-header { font-size: 14px; padding: 10px 12px; }
  .word-card { padding: 8px 12px; flex-direction: column; align-items: flex-start; }
  .word-english { font-size: 15px; }
  .word-japanese { font-size: 13px; }
  .play-btn { font-size: 13px; padding: 5px 10px; align-self: flex-end; margin-top: 4px; }
  .series-actions { flex-direction: row; gap: 8px; justify-content: center; }
  .btn-action { flex: 1 1 48%; font-size: 14px; padding: 10px 0; }
  .search-wrapper { flex-direction: column; gap: 8px; }
}
</style>

<script>
document.addEventListener("turbo:load", () => {
  let voices = [];

  function loadVoices(callback) {
    voices = speechSynthesis.getVoices();
    if (voices.length > 0) callback();
    else speechSynthesis.onvoiceschanged = () => { voices = speechSynthesis.getVoices(); callback(); };
  }

  function speakWord(text) {
    if (!text) return;
    speechSynthesis.cancel();
    const utterance = new SpeechSynthesisUtterance(text);
    utterance.lang = 'en-US';
    utterance.rate = 0.95;
    utterance.pitch = 1.0;
    const enVoice = voices.find(v => v.lang === 'en-US' && v.name.toLowerCase().includes('female')) 
                    || voices.find(v => v.lang.startsWith('en')) 
                    || voices[0];
    if (enVoice) utterance.voice = enVoice;
    speechSynthesis.speak(utterance);
  }

  loadVoices(() => {
    document.querySelectorAll(".play-btn").forEach(btn => {
      btn.addEventListener("click", () => {
        const word = btn.closest(".word-card").querySelector(".word-english").textContent.trim();
        speakWord(word);
      });
    });
  });
});
</script>
