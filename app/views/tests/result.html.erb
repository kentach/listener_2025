<h2 class="test-title">単語テスト結果</h2>

<div class="score-card">
  <p id="score" class="score-text"></p>
  <div class="score-bar-wrapper">
    <div class="score-bar-fill" id="score-bar"></div>
  </div>
</div>

<div class="results-container" id="results-container"></div>

<div class="btn-wrapper">
  <a href="<%= level_vocabularies_path(level: @test.level) %>" class="btn-retry">＜ 単語リストへ戻る</a>
</div>

<%= render "shared/navigation_tab" %>

<script>
document.addEventListener("turbo:load", () => {
  const results = JSON.parse(localStorage.getItem("testResults") || "[]");
  const container = document.getElementById("results-container");
  const scoreEl = document.getElementById("score");
  const scoreBar = document.getElementById("score-bar");

  let correctCount = 0;

  results.forEach((r, i) => {
    if(r.correct) correctCount++;

    const div = document.createElement("div");
    div.className = "result-card " + (r.correct ? "correct" : "wrong");
    div.innerHTML = `
      <div class="card-header">
        <span>Q${i+1}</span>
        <span>${r.correct ? "✅" : "❌"}</span>
      </div>
      <div class="card-body">
        <p><strong>問題:</strong> ${r.english}</p>
        <p><strong>正解:</strong> ${r.japanese}</p>
      </div>
    `;
    container.appendChild(div);
  });

  // スコア計算
  const total = results.length;
  const rate = total > 0 ? Math.round((correctCount / total) * 100) : 0;
  scoreEl.textContent = `正解数: ${correctCount} / ${total}｜${rate >= 80 ? "合格 🎉" : "不合格 😅"}`;

  // 進捗バーアニメーション
  let progress = 0;
  const timer = setInterval(() => {
    if (progress < rate) {
      progress++;
      scoreBar.style.width = progress + "%";
    } else {
      clearInterval(timer);
    }
  }, 8);

  // ページ更新してもデータを保持する場合は削除しない
  // localStorage.removeItem("testResults");
});
</script>

<style>
body {
  padding: 10px;
  box-sizing: border-box;
  background: #f9f9f9;
  font-family: "Helvetica Neue", Arial, sans-serif;
  color: #333;
  margin-bottom: 120px; /* 下のボタン分余白確保 */
}

/* タイトル */
.test-title {
  text-align: center;
  font-size: 36px;
  font-weight: 700;
  margin: 24px 0;
  color: #000;
}

/* スコアカード */
.score-card {
  max-width: 450px;
  width: 90%;
  margin: 0 auto 24px auto;
  background: #fff;
  border-radius: 20px;
  padding: 20px;
  box-shadow: 0 8px 20px rgba(0,0,0,0.1);
  text-align: center;
}

.score-text {
  font-size: 20px;
  font-weight: 700;
  margin-bottom: 12px;
  color: #2f6df6;
}

.score-bar-wrapper {
  width: 100%;
  height: 14px;
  background: #e0e0e0;
  border-radius: 7px;
  overflow: hidden;
}

.score-bar-fill {
  height: 100%;
  background: linear-gradient(90deg, #2f6df6, #4facfe);
  width: 0;
  border-radius: 7px;
  transition: width 0.8s ease;
}

/* 結果カード */
.results-container {
  max-width: 450px;
  width: 90%;
  margin: 0 auto 120px auto; /* 下にボタン分スペース */
}

.result-card {
  border-radius: 16px;
  padding: 16px;
  margin-bottom: 14px;
  box-shadow: 0 4px 10px rgba(0,0,0,0.06);
  text-align: left;
  transition: transform 0.2s, box-shadow 0.2s;
}

.result-card:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 14px rgba(0,0,0,0.1);
}

.result-card.correct {
  border-left: 6px solid #66bb6a;
  background: #e8f5e9;
}

.result-card.wrong {
  border-left: 6px solid #ef5350;
  background: #ffeaea;
}

.card-header {
  display: flex;
  justify-content: space-between;
  font-weight: 600;
  font-size: 14px;
  margin-bottom: 6px;
}

.card-body p {
  margin: 4px 0;
  font-size: 14px;
}

/* ボタン */
.btn-wrapper {
  position: fixed;
  bottom: 84px; /* 下端に近づける */
  left: 50%;
  transform: translateX(-50%);
  width: 90%;
  max-width: 450px;
  text-align: center;
  z-index: 100;
}

.btn-retry {
  display: inline-block;
  padding: 14px 24px;
  background: #2f6df6;
  color: #fff;
  border-radius: 20px;
  text-decoration: none;
  font-weight: 700;
  transition: background 0.2s;
}

.btn-retry:hover {
  background: #4facfe;
}

/* スマホ対応 */
@media screen and (max-width: 450px) {
  .score-card,
  .results-container,
  .btn-wrapper {
    width: 95%;
  }

  .score-text {
    font-size: 18px;
  }

  .card-body p {
    font-size: 13px;
  }

  .btn-retry {
    font-size: 16px;
    padding: 12px 20px;
  }
}
</style>
